import { StyleSheet } from "react-native";

export const shoppingListItemStyles = StyleSheet.create({
    item: {
        paddingTop: 8,
        paddingBottom: 8
    },
    itemText: {
        fontSize: 20,
        fontWeight: 'bold'
    }
});